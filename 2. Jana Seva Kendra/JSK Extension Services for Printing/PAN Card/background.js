// Background service worker for PAN Card Extension
// Base functionality ready for automations

chrome.runtime.onInstalled.addListener(() => {
    console.log("JSK PAN Card Automation Extension Installed!");
});
